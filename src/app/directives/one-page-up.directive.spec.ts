import { OnePageUpDirective } from './one-page-up.directive';

describe('OnePageUpDirective', () => {
  it('should create an instance', () => {
    const directive = new OnePageUpDirective();
    expect(directive).toBeTruthy();
  });
});
