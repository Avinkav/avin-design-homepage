import { OnePageSectionDirective } from './one-page-section.directive';

describe('OnePageSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new OnePageSectionDirective();
    expect(directive).toBeTruthy();
  });
});
