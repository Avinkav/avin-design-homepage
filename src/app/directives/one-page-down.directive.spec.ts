import { OnePageDownDirective } from './one-page-down.directive';

describe('OnePageDownDirective', () => {
  it('should create an instance', () => {
    const directive = new OnePageDownDirective();
    expect(directive).toBeTruthy();
  });
});
