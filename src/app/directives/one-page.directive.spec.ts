import { OnePageDirective } from './one-page.directive';

describe('OnePageDirective', () => {
  it('should create an instance', () => {
    const directive = new OnePageDirective();
    expect(directive).toBeTruthy();
  });
});
