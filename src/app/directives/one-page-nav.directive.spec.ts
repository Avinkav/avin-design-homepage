import { OnePageNavDirective } from './one-page-nav.directive';

describe('OnePageNavDirective', () => {
  it('should create an instance', () => {
    const directive = new OnePageNavDirective();
    expect(directive).toBeTruthy();
  });
});
